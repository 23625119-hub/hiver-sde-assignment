# Hiver SDE Intern Take-Home Assignment

## Overview

This project builds an AI-assisted customer support pipeline for AppleSupport.

The pipeline classifies customer messages, drafts support replies, and decides when human review is needed.

## Setup

```bash
pip install -r requirements.txt